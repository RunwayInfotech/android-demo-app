<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="10dp"
    android:background="@drawable/shadow">

    <TextView
        android:id="@+id/grid_item_label_no"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Item #"
        android:textAlignment="center"
        android:textStyle="italic"
        android:layout_marginBottom="5px"
        android:paddingTop="2dp"
        android:layout_centerHorizontal="true" />

    <ImageView
        android:id="@+id/grid_item_image"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:src="@drawable/icon_android"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/grid_item_label_no" />

    <TextView
        android:id="@+id/grid_item_label_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Title Here"
        android:layout_marginTop="5px"
        android:layout_below="@+id/grid_item_image"
        android:textStyle="bold"
        android:textAlignment="center"
        android:layout_centerHorizontal="true"
        android:paddingTop="5dp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="35dp"
        android:text="Read"
        android:textSize="12dp"
        android:layout_marginTop="7dp"
        android:layout_marginBottom="3dp"
        android:id="@+id/grid_item_button"
        android:layout_below="@+id/grid_item_label_title"
        android:layout_centerHorizontal="true"
        android:textColor="#FFFFFF"
        android:background="@color/avocarrot_green" />

</RelativeLayout>